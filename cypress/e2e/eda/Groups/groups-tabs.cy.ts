//Tests the actions a user can perform on the tabs inside of a Group in the EDA UI.

describe('EDA Groups Users Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the users in the Group and add a new user', () => {
    //write test here
  });

  it.skip('can filter the users associated with the Group', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can verify the functionality of items in the kebab menu on the Groups Users tab view', () => {
    //write test here
  });

  it.skip('can delete a user from the Group', () => {
    //write test here
  });
});
