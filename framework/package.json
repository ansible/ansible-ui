{
  "name": "@ansible/ansible-ui-framework",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "clean": "rm -rf coverage dist node_modules .eslintcache",
    "build": "npm run clean && vite build && tsc --project tsconfig.production.json"
  }
}
