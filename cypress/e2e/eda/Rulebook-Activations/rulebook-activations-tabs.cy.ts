//Tests the actions a user can perform on the tabs inside of a Rulebook Activation in the EDA UI.
// import { EdaRulebookActivation } from '../../../../frontend/eda/interfaces/EdaRulebookActivation';

describe('EDA Rulebook Activations History Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('renders the instances that are related to the rulebook activation', () => {
    //write test here
  });

  it.skip('can filter results on the history tab based on Name', () => {
    //write test here
  });
});
