//Tests the actions a user can perform on the tabs inside of a Rulebook listed in the EDA UI.

describe('EDA Rulebook Rulesets Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Rulesets Tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can verify the functionality of items in the kebab menu on the Rulesets list view', () => {
    //disable ruleset
  });
});

describe('EDA Rulebook > Ruleset > Rules Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Rules Tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});

describe('EDA Rulebook > Ruleset > Sources Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Sources Tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can click on a Source and view specific variables in the modal', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});
