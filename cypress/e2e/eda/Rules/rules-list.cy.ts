//Tests a user's ability to perform certain actions on the Rules list in the EDA UI.

describe('EDA Rules List', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Rules list based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can visit the details screen of a rule and verify the information shown', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});
