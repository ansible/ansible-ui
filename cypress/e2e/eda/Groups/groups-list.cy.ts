//Tests a user's ability to perform certain actions on the Groups list in the EDA UI.

describe('EDA Groups List', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Groups list based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can perform a basic search on the Groups list', () => {
    //use mock data to test search
  });

  it.skip('can perform an advanced search on the Groups list', () => {
    //use mock data to test search
  });

  it.skip('can bulk delete Groups from the list', () => {
    //write test here
  });

  it.skip('can verify the functionality of items in the kebab menu on the Groups list view', () => {
    //write test here
  });
});
