//Tests a user's ability to perform certain actions on the Users list in the EDA UI.

describe('EDA Users List', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Users list based on specific criteria', () => {
    //use mock data to test search
  });

  it.skip('can bulk delete Users from the list', () => {
    //write test here
  });

  it.skip('can verify the functionality of items in the kebab menu on the Users list view', () => {
    //restart rulebook activation, delete rulebook activation
  });

  it.skip('can view the groups that the user is associated with', () => {
    //write test here
  });
});
