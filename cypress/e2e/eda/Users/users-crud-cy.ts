//Tests a user's ability to create, edit, and delete Users in the EDA UI.

describe('EDA Users- Create, Edit, Delete', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can create a User, select groups to add the user to, and assert the information showing on the details page', () => {
    //write test here
  });

  it.skip('can edit a User including the groups the user belongs to', () => {
    //write test here
  });

  it.skip('can generate a new token for a User', () => {
    //write test here
  });

  it.skip('can delete a User', () => {
    //write test here
  });
});
