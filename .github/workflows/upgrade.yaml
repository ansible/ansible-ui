name: Upgrade
on:
    workflow_dispatch:
    schedule:
        - cron: '0 12 * * 0'
jobs:
    npm-check-updates:
        if: (github.event_name == 'schedule' && github.repository == 'jamestalton/ansible') || (github.event_name != 'schedule')
        runs-on: ubuntu-latest
        concurrency: ansible-ui
        steps:
            - uses: actions/checkout@v2
              with:
                  token: ${{ secrets.GH_TOKEN }}
            - uses: actions/setup-node@v2
              with:
                  node-version: '18'
                  registry-url: https://registry.npmjs.org/
                  cache: npm
            - run: npm run upgrade
            - uses: EndBug/add-and-commit@v7
              with:
                  default_author: github_actions
                  message: Upgraded package dependencies
