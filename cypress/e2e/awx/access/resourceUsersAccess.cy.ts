// import { Organization } from '../../../../frontend/awx/interfaces/Organization';
// import { AwxUser } from "../../../../frontend/awx/interfaces/User";

//Create a forLoop here to loop through resources
//Great possible example to follow: cypress/e2e/eda/Access-Management/user-access.cy.ts
/**Controller resources that need to be included in this testing:
 * Job Templates
 * Workflow Job Templates
 * Projects
 * Inventories
 * Credentials
 */
describe('User Access Tab for ${resource.name} - Add user', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it.skip('can add user role(s) via user access tab for ${resource.name}', () => {});
});

describe('User Access Tab for ${resource.name} - actions', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it.skip('can remove user role(s) from row of user access tab for ${resource.name}', () => {});

  it.skip('can bulk remove user roles from user access tab for ${resource.name}', () => {});
});
