name: Pull Request

on:
  workflow_call:
    secrets:
      COMPONENT_PROJECT_ID:
        required: true
      COMPONENT_RECORD_KEY:
        required: true
jobs:
  component-tests:
    name: Component Tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        task: [
          "component:awx:access",
          "component:awx:administration",
          "component:awx:common",
          "component:awx:overview",
          "component:awx:resources",
          "component:awx:views",
          "component:eda",
          "component:hub",
          "component:common",
          "component:framework"
        ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: PreCache
        run: npm version 0.0.0 --no-git-tag-version
      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            ./node_modules
            /home/runner/.cache/Cypress
          key: modules-${{ hashFiles('package-lock.json') }}
      - name: Install dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci
      - name: Run Cypress component tests ${{ matrix.task }}
        run: npm run ${{ matrix.task }}
