//Tests the actions a user can perform on the tabs inside of a Rulebook Activation in the EDA UI.

describe('EDA Rulebook Activations Actions Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Actions Tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can perform a basic search on the Actions Tab', () => {
    //need to use mock data to test the search feature
  });

  it.skip('can perform an advanced search on the Actions Tab', () => {
    //need to use mock data to test the search feature
  });
});

describe('EDA Rulebook Activations History Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the History tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can perform a basic search on the History tab', () => {
    //need to use mock data to test the search feature
  });

  it.skip('can perform an advanced search on the History tab', () => {
    //need to use mock data to test the search feature
  });
});
