{
  "scripts": {
    "test": "playwright test",
    "ui": "playwright test --ui",
    "test:chrome": "playwright test --project=chromium",
    "codegen:awx": "npx playwright codegen https://localhost:4101 --ignore-https-errors"
  }
}
