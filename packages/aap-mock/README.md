# aap-mock

## Overview

`aap-mock` is a mock implementation of the AAP API.

It is used for running pull request tests against a mock API to validate the code without having to setup a live AAP instance.
