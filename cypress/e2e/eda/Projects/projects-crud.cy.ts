//Tests a user's ability to create, edit, and delete a Project in the EDA UI.
/* eslint-disable @typescript-eslint/no-non-null-assertion */

describe('EDA Projects CRUD', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can create a Project, sync it, and assert the information showing on the details page', () => {
    //write test here
  });

  it.skip('can edit a project', () => {
    //write test here
  });

  it.skip('can delete a project', () => {
    //write test here
  });
});
