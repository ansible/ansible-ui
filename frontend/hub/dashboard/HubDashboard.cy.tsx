describe('HubDashboard', () => {
  it.skip('renders', () => {
    // TODO This would be a good place to test RBAC (platform admin vs regular user) use cases for the dashboard when we have the necessary APIs
  });
});
