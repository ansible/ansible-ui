//Tests a user's ability to log into and out of the EDA UI.
//Note that EDA Actions do not have any CRUD functionality.

describe('EDA Login / Logoff', () => {
  it.skip('can log into the UI and view username in the top right of the Dashboard toolbar', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('canlog out and login as a different user', () => {
    //need to use mock data to test the search feature
  });
});
