//Tests a user's ability to perform certain actions on the Decision Environment list in the EDA UI.

describe('EDA Decision Environment List', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Decision Environment list based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can bulk delete Decision Environments from the list', () => {
    //write test here
  });

  it.skip('can verify the functionality of items in the kebab menu on the EE list view', () => {
    //write test here
  });
});
