// import { Organization } from '../../../../frontend/awx/interfaces/Organization';
// import { Team } from '../../../../frontend/awx/interfaces/Team';

//Create a forLoop here to loop through resources
//Great possible example to follow: cypress/e2e/eda/Access-Management/team-access.cy.ts
/**Controller resources that need to be included in this testing:
 * Job Templates
 * Workflow Job Templates
 * Projects
 * Inventories
 * Credentials
 */
describe('Team Access Tab for ${resource.name} - Add team', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it.skip('can add team role(s) via team access tab for ${resource.name}', () => {});
});

describe('Team Access Tab for ${resource.name} - actions', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it.skip('can remove team role(s) from row of team access tab for ${resource.name}', () => {});

  it.skip('can bulk remove team roles from team access tab for ${resource.name}', () => {});
});
