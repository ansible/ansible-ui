//Tests a user's ability to create, edit, and delete Roles in the EDA UI.

describe('EDA Roles- Create, Edit, Delete', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can create a Role and assert the information showing on the details page', () => {
    //write test here
  });

  it.skip('can edit a role', () => {
    //write test here
  });

  it.skip('can delete a role', () => {
    //write test here
  });
});
