//Tests the actions a user can perform on the tabs inside of a Rulebook listed in the EDA UI.

describe('EDA Rulebook Rulesets Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can filter the Rulesets Tab based on specific criteria', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});
