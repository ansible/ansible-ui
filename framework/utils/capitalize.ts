export function capitalizeFirstLetter(text: string) {
  return text.charAt(0).toUpperCase() + text.slice(1);
}

export function capitalizeSecondLetter(text: string) {
  return text.charAt(1).toUpperCase() + text.slice(1);
}
