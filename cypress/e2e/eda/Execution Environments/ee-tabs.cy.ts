//Tests a user's ability to perform certain actions on the tabs inside an EE in the EDA UI.

describe('EDA Execution Environment Access Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the users who have access to the execution environment', () => {
    //write test here
  });
});

describe('EDA Execution Environment Instances Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the instances associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can filter the instances associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});

describe('EDA Execution Environment Repositories Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the repositories associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can filter the repositories associated with the Execution Environment', () => {
    //use mock data to test search
  });
});

describe('EDA Execution Environment Collections Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can add a Collection on the Execution Environment Collections tab and then view it on the list', () => {
    //use mock data to test search
  });

  it.skip('can filter the collections associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });
});

describe('EDA Execution Environment Contents Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the Contents associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can filter the Contents associated with the Execution Environment', () => {
    //use mock data to test search
  });
});

describe('EDA Execution Environment Dependencies Tab', () => {
  before(() => {
    cy.edaLogin();
  });

  it.skip('can view the Dependencies associated with the Execution Environment', () => {
    //change test stub name to stipulate what the specific criteria is
  });

  it.skip('can filter the Dependencies associated with the Execution Environment', () => {
    //use mock data to test search
  });
});
