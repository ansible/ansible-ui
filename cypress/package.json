{
  "name": "@ansible/cypress",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "concurrently npm:eslint npm:prettier npm:tsc -c auto",
    "tsc": "tsc --noEmit",
    "eslint": "eslint --max-warnings=0 .",
    "eslint:fix": "eslint --fix .",
    "prettier": "prettier --check !**/*.scss .",
    "prettier:fix": "prettier --write .",
    "fix": "concurrently npm:eslint:fix npm:prettier:fix -c auto"
  },
  "dependencies": {
    "@ansible/ansible-ui-framework": "../framework",
    "@ansible/awx-ui": "../frontend/awx",
    "@ansible/common-ui": "../frontend/common",
    "@ansible/eda-ui": "../frontend/eda",
    "@ansible/hub-ui": "../frontend/hub",
    "@ansible/platform-ui": "../platform",
    "@ansible/chatbot": "../frontend/chatbot"
  }
}
